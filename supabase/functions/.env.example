# Copy to supabase/functions/.env (git-ignored) for local `supabase functions
# serve`, or configure as Supabase Edge Function secrets for Preview/
# Production (`supabase secrets set`). Never commit real values.
PUBLIC_SITE_URL=
RESEND_API_KEY=
INVITE_EMAIL_FROM=
