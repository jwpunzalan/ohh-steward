{
  "name": "ohh-steward",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "test:rls": "vitest run tests/rls",
    "test:db": "vitest run tests/db"
  },
  "devDependencies": {
    "@supabase/supabase-js": "2.115.0",
    "vitest": "5.0.0"
  }
}
